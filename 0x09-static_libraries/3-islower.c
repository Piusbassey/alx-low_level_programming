#include "main.h"

/**
 * _islower - Checks for lowercase letters.
 * @c: The character to check.
 *
 * Return: 1 if c is lowercase, otherwise 0.
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
