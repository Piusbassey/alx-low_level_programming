#include <stdio.h>
#include "main.h"

/**
 * main - Entry point.
 * @i: Number to be printed.
 * Return: Always (0) success.
 */
int main(void)
{
	int i = 0;

	i = 0;
	positive_or_negative(i);
	return (0);
}
