#include "main.h"

/**
 * add - Adds two integers.
 * @a: First integer.
 * @b: Second integer.
 * Return: Returns the result of addition.
 */
int add(int a, int b)
{
	int result = a + b;
	return (result);
}
